# Vector,template 숙지 Vector은 배열이다
------------------

```c++


#include <vector>
template<typename T>
T Add(T InA, T InB)
{
	return InA + InB;
}

std::vector<float> Scores;
Scores.push_back(95.f);
std::cout << Add<int>(1, 2) << std::endl;
std::cout << Add<float>(1.f, 2.f) << std::endl;

```
추후 추가예정
